on:
  schedule:
    - cron: "0 4 * * *"
  workflow_dispatch:

name: docs

jobs:
  docs:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v5
        with:
          ref: main
          fetch-depth: 2

      - name: Extract Neovim section from README.md
        run: |
          mkdir -p /tmp/fff-docs
          awk '
            /^<details id="neovim-plugin">/ { capture=1; next }
            capture && /^<\/details>/        { capture=0; exit }
            capture && /^<summary>$/         { next }
            capture && /^<\/summary>$/       { next }
            capture && /<h2>.*<\/h2>/ {
              gsub(/<\/?h2>/, "")
              sub(/^[[:space:]]+/, "")
              print "# " $0
              print ""
              print "The best file search picker for Neovim. Frecency-ranked, typo-resistant, git-award, very fast."
              print ""
              next
            }
            capture { print }
          ' README.md > /tmp/fff-docs/neovim.md
          test -s /tmp/fff-docs/neovim.md

      - name: panvimdoc
        uses: kdheepak/panvimdoc@main
        with:
          vimdoc: fff.nvim
          pandoc: /tmp/fff-docs/neovim.md
          version: "Neovim >= 0.10.0"
          demojify: true
          treesitter: true

      - name: Create pull request
        id: cpr
        uses: peter-evans/create-pull-request@v7
        with:
          branch: bot/regenerate-vimdoc
          delete-branch: true
          title: "chore: regenerate Neovim vimdoc"
          commit-message: "chore: regenerate Neovim vimdoc"
          body: Automated vimdoc regeneration from README.md.
          add-paths: doc/fff.nvim.txt

      - name: Enable auto-merge
        if: steps.cpr.outputs.pull-request-number
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh pr merge --auto --squash "${{ steps.cpr.outputs.pull-request-number }}"
